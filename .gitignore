# ============ 环境与密钥 ============
# 忽略所有真实配置文件，包括分环境的覆盖层（.env.production / .env.development）。
# 只放行 .example 模板——它们没有真实值，是要给人看和复制的。
#
# 注意别把 `.env.*` 写成只匹配 `.env.local` 那几条：漏掉一个文件名就等于
# 把密钥提交进版本库，而这类事故在 push 之前几乎不可能被发现。
.env
.env.local
.env.*
!.env.example
!.env.*.example

# ============ Python / uv ============
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
ENV/
.python-version.bak
*.egg-info/
dist/
build/
.uv/

# ============ 测试与覆盖率 ============
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.mypy_cache/
.ruff_cache/

# ============ 运行时数据 ============
data/
uploads/
*.log
logs/

# ============ Node / pnpm ============
node_modules/
.pnpm-store/
frontend/dist/
frontend/dist-ssr/
frontend/stats.html
*.tsbuildinfo
.eslintcache
.vite/

# ============ 编辑器 / 操作系统 ============
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~

# ============ Docker ============
# 本地覆盖文件（含个人配置），不进版本库
docker-compose.override.yml
