# ============================================================================
# 这一条是必须的，删掉就会构建失败。
#
# `COPY . .` 会把宿主机的 node_modules 覆盖掉容器里刚装好的依赖。
# 宿主机是 macOS arm64、容器是 Linux，原生模块（rollup / esbuild 的二进制）
# 平台不匹配，报错形如：
#   Cannot find module @rollup/rollup-linux-arm64-gnu
# 而报错信息完全指不到真正的原因。
# ============================================================================
node_modules/

# 构建产物由容器内重新生成
dist/

# 环境文件。当前 frontend/ 下没有 .env（配置统一走仓库根的 .env），
# 所以这一条暂时不起作用——它防的是将来有人往这里放一个 .env 的情况。
# 那种文件会被 COPY 进镜像层，密钥就此留在构建产物里，且事后很难发现。
# 后端那份 .dockerignore 已有同样的规则，两边保持一致。
.env
.env.*

# 编辑器与系统文件
.DS_Store
.idea/
.vscode/
*.swp

# 日志与缓存
*.log
.eslintcache
.vite/

# 生成物会在构建时由 openapi-typescript 之外的流程处理，
# 这里保留 src/api/types.gen.ts —— 它是提交进仓库的，构建时需要它
